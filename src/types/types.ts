// news card props
export type NewsCardProps = {
  title: string;
  date: string;
  description: string;
  image: string;
};

// story card props
export type StoryCardProps = {
  title: string;
  subtext: string;
  link: string;
  className?: string;
  image: string;
};
